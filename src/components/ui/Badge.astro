---
const { label, href, active = false } = Astro.props;
---

<a
  href={href}
  class={`
    inline-block px-3 py-1 text-xs font-medium rounded-full transition
    ${active
      ? "bg-black text-white"
      : "bg-gray-100 text-gray-700 hover:bg-gray-200"}
  `}
>
  {label}
</a>
